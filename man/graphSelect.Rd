% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graphSI.R
\name{graphSelect}
\alias{graphSelect}
\title{Regularized undirected graph selection}
\usage{
graphSelect(
  data,
  lambda = NULL,
  gamma = NULL,
  data.splitting = FALSE,
  split.proportion = NULL,
  loss = c("Gaussian"),
  penalty = c("lasso", "elastic net", "SCAD", "MCP"),
  penalize.diagonal = FALSE,
  seed = NULL
)
}
\arguments{
\item{data}{Data matrix (n x p)}

\item{lambda}{Tuning parameter for the penalty (default: sqrt(log(p)/n))}

\item{gamma}{Additional tuning parameter for certain penalties (default: 0.5 for elastic net, 3.7 for SCAD, 2.0 for MCP)}

\item{data.splitting}{Logical indicating whether to use data splitting for inference (default: FALSE)}

\item{split.proportion}{Proportion of data to use for selection if data.splitting is TRUE (default: 0.5)}

\item{loss}{Loss function to use (default: "Gaussian")}

\item{penalty}{Penalty to use for graph selection (default: "lasso")}

\item{penalize.diagonal}{Logical indicating whether to penalize diagonal elements (default: FALSE)}

\item{seed}{Random seed for data splitting (default: NULL)}
}
\value{
An S3 object of class 'graphSelect' with user-facing elements: adjacency matrix, selected indices, data splitting info. Internal elements also stored but hidden from print().
}
\description{
Select an undirected graphical model using regularized estimation methods.
}
